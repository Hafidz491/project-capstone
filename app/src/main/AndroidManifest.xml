<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dicoding.picodiploma.sigfood">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SigFood">
        <activity
            android:name=".UI.category.CategoryActivity"
            android:exported="false" />
        <activity
            android:name=".UI.detail.DetailActivity"
            android:exported="false" />
        <activity
            android:name=".UI.signup.SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".UI.login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".UI.loginfirst.LoginFirstActivity"
            android:exported="false" />
        <activity
            android:name=".UI.container.ContainerActivity"
            android:exported="false"
            android:label="@string/title_activity_container" />
        <activity
            android:name=".UI.landing.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>